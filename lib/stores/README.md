Zustand stores for client-side UI state (form progress, modal state).
First consumer: the Generate flow store in Phase 5.
Never put secrets or server-only values here — this code ships to the browser.
